# Context map (Python)

## Load order recomendado
1. `AGENTS.md` del servicio (o `README.md` si no existe).
2. `.ai-kit/references/python-rules.md`.
3. HU: `context/hu/<HU_ID>/` (contrato + plan).
4. Configs: `pyproject.toml`, `requirements.txt`, `template.yaml` o manifests del servicio.

